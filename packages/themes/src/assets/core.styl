// Styles necessary for Lextrix

LIST_STYLE = decimal lower-alpha lower-roman
LIST_STYLE_WIDTH = 1.2em
LIST_STYLE_MARGIN = 0.3em
LIST_STYLE_OUTER_WIDTH = LIST_STYLE_MARGIN + LIST_STYLE_WIDTH
MAX_INDENT = 9

resets(arr)
  unquote('list-' + join(' list-', arr))

.lxr-container
  box-sizing: border-box
  font-family: Helvetica, Arial, sans-serif
  font-size: 13px
  height: 100%
  margin: 0px
  position: relative

.lxr-container.lxr-disabled
  .lxr-tooltip
    visibility: hidden

.lxr-container:not(.lxr-disabled)
  li[data-list=checked],
  li[data-list=unchecked]
    > .lxr-ui
      cursor: pointer

.lxr-clipboard
  left: -100000px
  height: 1px
  overflow-y: hidden
  position: absolute
  top: 50%
  p
    margin: 0
    padding: 0

.lxr-editor
  box-sizing: border-box
  counter-reset: resets(0..MAX_INDENT)
  line-height: 1.42
  height: 100%
  outline: none
  overflow-y: auto
  padding: 12px 15px
  position: relative
  tab-size: 4
  -moz-tab-size: 4
  text-align: left
  white-space: pre-wrap
  word-wrap: break-word
  > *
    cursor: text

  p, ol, pre, blockquote, h1, h2, h3, h4, h5, h6
    margin: 0
    padding: 0
  p, h1, h2, h3, h4, h5, h6
    @supports (counter-set: none)
      counter-set: resets(0..MAX_INDENT)
    @supports not (counter-set: none)
      counter-reset: resets(0..MAX_INDENT)
  table
    border-collapse: collapse
  td
    border: 1px solid #000
    padding: 2px 5px
  ol
    padding-left: 1.5em
  li
    list-style-type: none
    padding-left: LIST_STYLE_OUTER_WIDTH
    position: relative

    > .lxr-ui:before
      display: inline-block
      margin-left: -1*LIST_STYLE_OUTER_WIDTH
      margin-right: LIST_STYLE_MARGIN
      text-align: right
      white-space: nowrap
      width: LIST_STYLE_WIDTH

  li[data-list=checked],
  li[data-list=unchecked]
    > .lxr-ui
      color: #777

  li[data-list=bullet] > .lxr-ui:before
    content: '\2022'
  li[data-list=checked] > .lxr-ui:before
    content: '\2611'
  li[data-list=unchecked] > .lxr-ui:before
    content: '\2610'

  li[data-list]
    @supports (counter-set: none)
      counter-set: resets(1..MAX_INDENT)
    @supports not (counter-set: none)
      counter-reset: resets(1..MAX_INDENT)

  li[data-list=ordered]
    counter-increment: list-0
    > .lxr-ui:before
      content: unquote('counter(list-0, ' + LIST_STYLE[0] + ')') '. '
  for num in (1..MAX_INDENT)
    li[data-list=ordered].lxr-indent-{num}
      counter-increment: unquote('list-' + num)
      > .lxr-ui:before
        content: unquote('counter(list-' + num + ', ' + LIST_STYLE[num%3] + ')') '. '
    if (num < MAX_INDENT)
      li[data-list].lxr-indent-{num}
        @supports (counter-set: none)
          counter-set: resets((num+1)..MAX_INDENT)
        @supports not (counter-set: none)
          counter-reset: resets((num+1)..MAX_INDENT)

  for num in (1..MAX_INDENT)
    .lxr-indent-{num}:not(.lxr-direction-rtl)
      padding-left: (3*num)em
    li.lxr-indent-{num}:not(.lxr-direction-rtl)
      padding-left: (3*num + LIST_STYLE_OUTER_WIDTH)em
    .lxr-indent-{num}.lxr-direction-rtl.lxr-align-right
      padding-right: (3*num)em
    li.lxr-indent-{num}.lxr-direction-rtl.lxr-align-right
      padding-right: (3*num + LIST_STYLE_OUTER_WIDTH)em

  li.lxr-direction-rtl
    padding-right: LIST_STYLE_OUTER_WIDTH
    > .lxr-ui:before
      margin-left: LIST_STYLE_MARGIN
      margin-right: -1*LIST_STYLE_OUTER_WIDTH
      text-align: left

  table
    table-layout: fixed
    width: 100%
    td
      outline: none

  .lxr-code-block-container
    font-family: monospace

  .lxr-video
    display: block
    max-width: 100%
  .lxr-video.lxr-align-center
    margin: 0 auto
  .lxr-video.lxr-align-right
    margin: 0 0 0 auto

  .lxr-bg-black
    background-color: rgb(15, 23, 42)
  .lxr-bg-red
    background-color: rgb(239, 68, 68)
  .lxr-bg-orange
    background-color: rgb(249, 115, 22)
  .lxr-bg-yellow
    background-color: rgb(234, 179, 8)
  .lxr-bg-green
    background-color: rgb(34, 197, 94)
  .lxr-bg-blue
    background-color: rgb(59, 130, 246)
  .lxr-bg-purple
    background-color: rgb(139, 92, 246)

  .lxr-color-white
    color: rgb(255, 255, 255)
  .lxr-color-red
    color: rgb(239, 68, 68)
  .lxr-color-orange
    color: rgb(249, 115, 22)
  .lxr-color-yellow
    color: rgb(234, 179, 8)
  .lxr-color-green
    color: rgb(34, 197, 94)
  .lxr-color-blue
    color: rgb(59, 130, 246)
  .lxr-color-purple
    color: rgb(139, 92, 246)

  .lxr-font-serif
    font-family: Georgia, Times New Roman, serif
  .lxr-font-monospace
    font-family: Monaco, Courier New, monospace

  .lxr-size-small
    font-size: 0.75em
  .lxr-size-large
    font-size: 1.5em
  .lxr-size-huge
    font-size: 2.5em

  .lxr-direction-rtl
    direction: rtl
    text-align: inherit

  .lxr-align-center
    text-align: center
  .lxr-align-justify
    text-align: justify
  .lxr-align-right
    text-align: right

  .lxr-ui
    position: absolute

.lxr-editor.lxr-blank::before
  color: rgba(0,0,0,0.6)
  content: attr(data-placeholder)
  font-style: italic
  left: 15px
  pointer-events: none
  position: absolute
  right: 15px
